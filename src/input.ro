# foo(x int, y int, h int, $caller_line)
  - a = 42
  - b = "foo"
  print("caller line: ", $caller_line)
  - c = 3.14
  - caller_line int
  ---
    - x int

# bar()
  print("bar")
  nop

// twice the number
# twice(i int) int
  return 2 * i

# main() int
  bar()
  foo(33, 44, 55)
  return twice(3)
